# ⚠️ سُپابيس غير مُعد Setup Required

## المشكلة الحالية:
التطبيق يحتاج إلى Supabase credentials للعمل بشكل صحيح.

## الحل - خيارين:

### الخيار 1️⃣: إعداد Supabase (موصى به) 🚀

#### الخطوة 1: إنشاء مشروع Supabase
1. اذهب إلى [supabase.com](https://supabase.com)
2. سجل حساب جديد أو قم بتسجيل الدخول
3. اضغط "New Project"
4. املأ المعلومات:
   - Name: halal-fin-saas
   - Database Password: (احفظها جيداً)
   - Region: اختر أقرب منطقة
5. اضغط "Create new project"
6. انتظر 2-3 دقائق حتى يتم الإنشاء

#### الخطوة 2: تطبيق Database Schema
1. في مشروع Supabase، اذهب إلى **SQL Editor**
2. افتح ملف `supabase/schema.sql` من مشروعك
3. انسخ المحتوى بالكامل
4. الصقه في SQL Editor
5. اضغط **"Run"**

#### الخطوة 3: الحصول على Credentials
1. اذهب إلى **Settings** → **API**
2. انسخ:
   - **Project URL** → هذا هو `VITE_SUPABASE_URL`
   - **anon public key** → هذا هو `VITE_SUPABASE_ANON_KEY`

#### الخطوة 4: إضافة Credentials محلياً
1. في مجلد المشروع، أنشئ ملف `.env`:
```bash
cd d:\ai\halal-fin-saas
cp .env.example .env
```

2. افتح `.env` وأضف:
```
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi....
```

#### الخطوة 5: إضافة Credentials على Vercel
1. اذهب إلى [vercel.com](https://vercel.com)
2. افتح مشروع `halal-fin-saas`
3. اذهب إلى **Settings** → **Environment Variables**
4. أضف:
   - `VITE_SUPABASE_URL` = قيمتها
   - `VITE_SUPABASE_ANON_KEY` = قيمتها
5. اضغط **Save**
6. اضغط **Redeploy** لتطبيق التغييرات

---

### الخيار 2️⃣: العمل بدون Supabase (تجريبي)

إذا كنت تريد فقط تجربة التطبيق بدون قاعدة بيانات حقيقية:

1. أنشئ ملف `.env` بقيم وهمية:
```
VITE_SUPABASE_URL=https://demo.supabase.co
VITE_SUPABASE_ANON_KEY=demo_key
```

2. سأعدل الكود ليعمل في "Mock Mode" تلقائياً

**ملاحظة**: في هذه الحالة لن تحفظ البيانات حقيقياً.

---

## ✅ التحقق من النجاح

بعد الإعداد:
1. أعد تشغيل التطبيق محلياً: `npm run dev`
2. جرب إنشاء حساب جديد
3. يجب أن يعمل بدون "Failed to fetch"

---

**أي خيار تريد؟**
- إذا تريد خيار 1 (Supabase الحقيقي): قل "سأعد Supabase"
- إذا تريد خيار 2 (Mock Mode): قل "اعمل Mock Mode"
