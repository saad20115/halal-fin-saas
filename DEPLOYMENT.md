# خطوات نشر المشروع على الإنترنت 🚀

## المتطلبات الأساسية:
1. حساب على GitHub (مجاني)
2. حساب على Vercel (مجاني)

---

## الخطوة 1️⃣: رفع المشروع على GitHub

### 1. إنشاء Repository جديد على GitHub:
- اذهب إلى [github.com/new](https://github.com/new)
- اسم الـ Repository: `halal-fin-saas`
- اجعله Public أو Private حسب رغبتك
- **لا تضف** README أو .gitignore (موجودين بالفعل)
- اضغط "Create repository"

### 2. تنفيذ الأوامر التالية في Terminal:

```bash
# الانتقال لمجلد المشروع
cd d:\ai\halal-fin-saas

# تهيئة Git (إذا لم يكن مهيئاً)
git init

# إضافة جميع الملفات
git add .

# عمل Commit أولي
git commit -m "Initial commit: Halal Financial SaaS Platform"

# تسمية الفرع الرئيسي
git branch -M main

# ربط المشروع بـ GitHub (استبدل YOUR_USERNAME باسم حسابك)
git remote add origin https://github.com/YOUR_USERNAME/halal-fin-saas.git

# رفع المشروع
git push -u origin main
```

**ملاحظة**: عند الـ push، قد يطلب منك تسجيل الدخول لحساب GitHub.

---

## الخطوة 2️⃣: النشر على Vercel

### 1. الذهاب إلى Vercel:
- اذهب إلى [vercel.com](https://vercel.com)
- اضغط "Sign Up" أو "Continue with GitHub"
- سجل الدخول بحساب GitHub

### 2. إنشاء مشروع جديد:
- اضغط "Add New..." ثم "Project"
- اختر `halal-fin-saas` من قائمة المشاريع
- اضغط "Import"

### 3. التهيئة:
Vercel سيكتشف تلقائياً أنه مشروع Vite، لكن تأكد من:
- **Framework Preset**: Vite
- **Build Command**: `npm run build`
- **Output Directory**: `dist`

### 4. إضافة متغيرات البيئة (Environment Variables):
اضغط "Environment Variables" وأضف:

```
VITE_SUPABASE_URL = [ضع رابط Supabase الخاص بك]
VITE_SUPABASE_ANON_KEY = [ضع مفتاح Supabase]
```

### 5. النشر:
- اضغط "Deploy"
- انتظر 2-3 دقائق

---

## الخطوة 3️⃣: الحصول على رابط المشروع

بعد انتهاء النشر:
- سترى رسالة "Congratulations!"
- الرابط سيكون بهذا الشكل: `https://halal-fin-saas.vercel.app`
- **انسخ الرابط وشاركه!** 🎉

---

## ⚙️ إعداد Supabase (إذا لم تفعل بعد):

1. اذهب إلى [supabase.com](https://supabase.com)
2. أنشئ مشروع جديد
3. اذهب إلى **SQL Editor**
4. افتح ملف `supabase/schema.sql` من المشروع
5. انسخ المحتوى والصقه في SQL Editor
6. اضغط "Run"
7. اذهب إلى **Settings > API**
8. انسخ:
   - `Project URL` → `VITE_SUPABASE_URL`
   - `anon public` key → `VITE_SUPABASE_ANON_KEY`

---

## 🔄 التحديثات المستقبلية

عندما تريد تحديث المشروع:

```bash
git add .
git commit -m "وصف التحديث"
git push
```

Vercel سيقوم بالنشر تلقائياً! ✨

---

## 🆘 حل المشاكل الشائعة

### المشكلة: `git push` يطلب اسم مستخدم وكلمة مرور
**الحل**: استخدم Personal Access Token بدلاً من كلمة المرور:
1. اذهب إلى GitHub Settings > Developer settings > Personal access tokens
2. أنشئ Token جديد
3. استخدمه بدلاً من كلمة المرور

### المشكلة: الموقع يعمل محلياً لكن لا يعمل على Vercel
**الحل**: تأكد من إضافة متغيرات البيئة على Vercel

---

## ✅ التأكد من النجاح

افتح رابط Vercel من أي جهاز:
- ✓ يجب أن تظهر الصفحة الرئيسية
- ✓ تبديل اللغة يعمل (EN/AR)
- ✓ جميع الصفحات تعمل

---

**تهانينا! مشروعك الآن على الإنترنت! 🎊**
